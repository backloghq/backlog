{
  "mcpServers": {
    "taskwarrior": {
      "command": "${CLAUDE_PLUGIN_ROOT}/scripts/start-server.sh",
      "env": {
        "TASKDATA_ROOT": "${CLAUDE_PLUGIN_DATA}/projects"
      }
    }
  }
}
